syntax = "proto3";

package raptor.v1;

enum RelayState {
  RELAY_STATE_UNSPECIFIED = 0;
  RELAY_STATE_OPENED = 1;
  RELAY_STATE_CLOSED = 2;
}

message Relays {
  RelayState esc_power = 1;
  RelayState battery_power = 2;
  RelayState ext_power = 3;
}
